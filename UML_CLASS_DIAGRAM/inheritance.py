"""
    Inheritance (or Generalization) represents an "Is-A" relationship where one class (subclass)
    inherits the attributes and methods of another class (superclass).

    Example: A Dog class and a Cat class inherit from an Animal class, as both dogs and cats are animals.
"""